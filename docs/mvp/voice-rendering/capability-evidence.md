# Voice Rendering capability evidence

Twenty domain tests prove exact voice-policy parsing, Translation Manifest ordering, bounded Edge concurrency, one active Qwen batch, hash-bound clip publication, failed-clip isolation, batch-to-serial recovery, successful checkpoint reuse, duplicate replay, input conflict, provider construction, duration probing and public CLI behavior.

Live evidence on 2026-08-02 used `ru-RU-DmitryNeural` and `kk-KZ-DauletNeural`. The first service run completed three clips and failed one; the identical retry synthesized only the failed Russian clip and reused the other three. The final four clips total 23.232 seconds and the Voice Manifest SHA-256 is `ccbfd566073f1b548fe02a9eb01fedda6be5f6a77af37575f1e786b5db802d56`.

Performance evidence on 2026-08-03 uses the real 129-segment Russian run `f69c28bb-0547-4391-81c6-3411aa14685e`. The prior Qwen loop spent 1,352.38 seconds in Voice Rendering, including 209.48 seconds for model initialization plus the first clip and an 8.88-second average for later clips. The post-fix full run completed 129/129 clips with zero failures in 285.18 seconds on the same RTX 4070 Laptop GPU: a measured 4.74x end-to-end improvement. An exact comparison of the Translation JSON and Voice Receipt confirmed all 129 segment IDs, start/end times and translated texts were preserved, and every published clip exists with a positive probed duration. A focused eight-item batch loaded the cached model in 13.99 seconds and generated eight independent waveforms in 12.43 seconds, or 1.55 seconds per item.

Graph Studio tests prove ordered owner steps, exact selected-language voice coverage, public adapter registration, clip hash/size/duration verification and browser controls for creator and folder workflows. The same live creator run completed 129/129 Qwen clips and one 217.866-second localized derivative before the batching change.
