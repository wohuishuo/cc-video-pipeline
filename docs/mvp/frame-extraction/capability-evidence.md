# Frame Extraction capability evidence

The independent launcher and dependency contract are manifest-verified. The existing interval and cut-aligned implementation remains in use. Output manifest verification is missing.
