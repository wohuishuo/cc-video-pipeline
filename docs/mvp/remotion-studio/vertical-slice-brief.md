# Remotion Studio vertical slice brief

Observable result: list, preview, or render a registered composition using caller-supplied project data. The registry owns composition metadata; project assets remain project-owned.
