# Remotion Studio capability evidence

The independent installer and studio/render entrypoint are manifest-verified. Existing compositions remain behind the adapter. Registry metadata and asset-isolation tests remain missing.
