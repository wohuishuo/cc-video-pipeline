# Creator Selection capability evidence

- Contract tests prove strict Creator Manifest parsing, supported-platform URL matching and exact video identity.
- Operation tests prove source-order preservation, unknown/duplicate/empty rejection, same-operation replay and changed-input conflict.
- Public launcher evidence invokes `run.ps1` from an unrelated working directory and commits a readable Selection Manifest.
- Studio composition tests prove the browser submits a discovery run ID plus selected IDs, never a trusted artifact path, and Creator Batch receives only the committed Selection fact.

No live media download or publication is claimed because this capability owns neither effect.
