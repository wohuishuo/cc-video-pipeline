# YouTube OAuth Bootstrap implementation plan

1. Add failing client-config, PKCE/state, token-exchange, Vault-boundary and receipt tests.
2. Implement the independent loopback CLI and redacted result contract.
3. Add a two-node Studio YouTube Connect Graph and browser inputs.
4. Verify deterministic substitutes, launcher behavior and complete repository suite.
5. Record evidence limits and merge the slice to main.
