# Resource-aware Studio implementation plan

1. Add failing Resource Budget reactivation and Studio lease lifecycle tests.
2. Add expired stable-ID reactivation to Resource Budget without weakening fingerprint conflicts.
3. Add the public Resource Budget command adapter and renewable lease coordinator.
4. Compose acquisition, durable requeue, heartbeat fencing, terminal release and startup reconciliation into Workflow Engine.
5. Expose opt-in fixed and multi-workspace launcher configuration.
6. Run a real CLI/Studio drill, update architecture/product/training/evidence, and run the full suite.
