# Localization MVP

Coordinates transcript translation, dubbed speech, timing alignment, and subtitles through explicit files. It does not own voice identities or transcription models.

```powershell
.\install.ps1
.\run.ps1 --help
```
