# Channel Research MVP

Owns a reproducible research workspace for source videos, acquisition facts, transcripts, measurements, and dossiers. Platform access is replaceable behind adapters.

```powershell
.\install.ps1
.\run.ps1 --workspace research-workspace create demo:video-1
```
