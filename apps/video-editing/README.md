# Video Editing MVP

Creates a derivative without overwriting its input. Available operations are silence cutting, vertical conversion, and face-aware reframing.

```powershell
.\install.ps1
.\run.ps1 silence-cut -Video input.mp4 -Out output.mp4
.\run.ps1 vertical -Video input.mp4 -Out vertical.mp4 -Mode blur
```
