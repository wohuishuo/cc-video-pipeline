# Frame Extraction MVP

Extracts frames at fixed intervals or supplied cut points. The program owns only the resulting frame set.

```powershell
.\install.ps1
.\run.ps1 -Video input.mp4 -OutDir frames
```
